Bhanu Prakash Pamidi -800972464

1.The Algorithm first reads the contents of file and takes everything into string ,then it parses the string to form the 2D array.
2.Now the 2D array is traversed to find all local peaks in it .

Note: indexes mentioned in output file start with zero